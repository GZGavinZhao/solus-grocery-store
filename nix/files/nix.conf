#
# https://nixos.org/manual/nix/stable/#sec-conf-file
#

# Unix group containing the Nix build user accounts
build-users-group = nixbld

# Enabl flakes by default
experimental-features = nix-command flakes

# Disable sandbox
# sandbox = false
